<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.belgacom.fon"
    android:installLocation="auto"
    android:versionCode="0018"
    android:versionName="1.2.5" >

    <uses-sdk
      android:minSdkVersion="10"
      android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature
        android:name="android.hardware.wifi"
        android:required="true" />

    <application
        android:debuggable="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:theme="@style/FONTheme" >
        <activity
            android:name=".activity.HomeActivity"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.PreferencesAdvanced"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Light.NoTitleBar" />
        <activity
            android:name=".activity.HelpListActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.AccountActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.WebActivity"
            android:screenOrientation="portrait" />

        <receiver android:name=".WifiScanReceiver" >
            <intent-filter>
                <action android:name="android.net.wifi.SCAN_RESULTS" />
            </intent-filter>
        </receiver>
        <receiver android:name=".connectivity.checker.InternetCheckAlarmReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>
        <receiver android:name=".connectivity.RouterConnectionStateChangeReceiver" >
            <intent-filter>
                <action android:name="android.net.wifi.STATE_CHANGE" />
            </intent-filter>
        </receiver>
        <receiver android:name=".ResetStatusReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.net.wifi.WIFI_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <provider
            android:name=".blacklist.FonBlackListProvider"
            android:authorities="com.belgacom.fon.blacklist.FonBlackListProvider" />
        <provider
            android:name=".connectivity.FonNetworkListProvider"
            android:authorities="com.belgacom.fon.connectivity.FonNetworkListProvider" />

        <service android:name=".notification.NotificationHandlerService" />
        <service android:name=".connectivity.FonConnectService" />
        <service android:name=".connectivity.checker.FonCheckConnectService" />
        <service android:name=".connectivity.RouterConnectService" />
        <service android:name=".ResetStatusService" />
    </application>

</manifest>